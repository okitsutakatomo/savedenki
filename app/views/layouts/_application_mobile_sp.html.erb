<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1, maximum-scale=1"> 
    <title><%= t "layouts.application.header_title" %></title>
    <link rel="stylesheet" href="/stylesheets/reset.css">
    <link rel="stylesheet" href="/stylesheets/base.css">

    <%= javascript_include_tag :defaults %>
    <script src="http://platform.twitter.com/widgets.js" type="text/javascript"></script>
    <meta name="format-detection" content="telephone=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">

    <link rel="start" href="/" title="<%= t "layouts.application.start_title" %>">
    <link rel="apple-touch-icon" href="/images/apple-touch-icon.png">
    <meta name="description" content="<%= t "layouts.application.description" %>">
    <meta name="keywords" content="<%= t "layouts.application.keywords" %>">
    <meta name="author" content="mimiuchi project">
  </head>
  <body>
    <!--
    <div id="header_announce">
      <%= t "layouts.application.english_announce" %>
    </div>
    -->
    <header id="header">
    <h1 id="logo"><%= link_to t("layouts.application.logo_link_text"), root_path %></h1>
    <% if logged_in? -%>
      <div id="whois">
        <img src="<%= current_user.profile_image_url %>" />
        <p><span id="login_id">@<%= current_user.screen_name %></span><br /><%= t "layouts.application.login_information" %></p>
      </div>
    <% else %>
      <form method="get" action="/locales/index" id="locale_form">
        <select name="locale" id="language">
        <% if I18n.locale == :ja %>
          <option value="ja" selected="true"><%= t "account.index.locale_ja_label" %></option>
        <% else %>
          <option value="ja"><%= t "account.index.locale_ja_label" %></option>
        <% end %>
        <% if I18n.locale == :en %>
          <option value="en" selected="true"><%= t "account.index.locale_en_label" %></option>
        <% else %>
          <option value="en"><%= t "account.index.locale_en_label" %></option>
        <% end %>
        </select>
      </form>
    <% end %>
    </header>

    <div id="content">
      <% if logged_in? -%>
        <p id="setting_link"><%= link_to t("layouts.application.account_link_text"), "/account" %></p>
      <% end %>

      <% if has_new_coupleinfo?(current_user) %>
        <p class="info couple_new_info"><a href="/home"><%= t "layouts.application.couple_notification" %></a></p>
      <% end %>

      <% if has_new_onewayinfo?(current_user) %>
        <p class="info oneway_new_info"><a href="/home"><%= t "layouts.application.oneway_notifications" %></a></p>
      <% end %>

      <% if !notice.blank? %>
        <p class="flash"><%= notice %></p>
      <% end %>

      <% if logged_in? -%>
        <p id="homelink" class="button"><a href="/home"><%= t "layouts.application.home_link_title" %></a></p>
        <% unless Love.find_with_deleted(:all, :conditions => "user_id = #{current_user.id}").blank? %>
          <!--<p class="infoannounce"><a href="/about/delivery"><%= t "layouts.application.delivery_link_title" %></a></p>-->
        <% end %>
      <% end %>

      <%= yield %>
    </div>

    <footer id="footer">
    <div id="footer_nav">
      <ul>
        <% if logged_in? -%>
          <li><%= link_to t("layouts.application.logout_link_text"), logout_path %></li>
        <% end -%>
        <li><%= link_to t("layouts.application.help_link_text"), help_path %></li>
        <li class="last"><%= link_to t("layouts.application.about_link_text"), about_path %></li>
      </ul>
    </div>
    <div id="gradient_border"></div>
    <div id="footer_grad">
    </div>
    <div id="footer_ad">
    </div>
    <p id="copyright"><%= t "layouts.application.copyright" %></p>
    </footer>

    <!-- Scripts [[[-->
    <!-- common scripts [-->
    <!--] common scripts -->
    <!--]]] Scripts-->

    <!--Analytics [-->
    <% if RAILS_ENV == "production" %>
      <%= render :partial => "commons/ga" %>
    <% end %>
    <!--] Analytics-->

  </body>
</html>


